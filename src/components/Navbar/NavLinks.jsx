import React, { useState } from "react";
import { fetcher } from "@/utils/API";
import Link from "next/link";

export default function NavLinks({ genres }) {
  return <div className="text-indigo-900">{console.log(genres)}</div>;
}
